import logging

logging.basicConfig(level=logging.INFO)

if __name__ == "__main__":
    """
    -DataFrame是数据分析的主力
    """
    logging.info(
        f"-----------------------------构建------------------------------------------"
    )
